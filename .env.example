# SitePlod Backend Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# Supabase Configuration (REQUIRED)
# ============================================
# Your Supabase project URL
# Get this from: Supabase Dashboard > Project Settings > API
SUPABASE_URL=
# Supabase anonymous key (public key)
# Used for client-side authentication
# Get this from: Supabase Dashboard > Project Settings > API > anon/public
SUPABASE_ANON_KEY=
# Supabase service role key (private key)
# Used for server-side operations with elevated privileges
# KEEP THIS SECRET! Never expose in client-side code
# Get this from: Supabase Dashboard > Project Settings > API > service_role
SUPABASE_SERVICE_KEY=
# ============================================
# External Storage Services (REQUIRED)
# ============================================
# Pastebin API key for storing HTML/CSS/JS files
# Get this from: https://pastebin.com/doc_api#1
# You need a Pastebin account to generate an API key
# Tip: Provide multiple keys separated by commas if you hit rate limits frequently
PASTEBIN_API_KEYS=key1,key2,key3
# ImgBB API key for storing images
# Get this from: https://api.imgbb.com/
# Free tier: 5000 requests per month
IMGBB_API_KEY=
# ============================================
# Application Configuration (REQUIRED)
# ============================================
# Base URL of your application
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# JWT secret for token signing
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# ============================================
# Email Configuration (OPTIONAL)
# ============================================
# SMTP credentials for sending emails (e.g., welcome emails, password resets)
# For Gmail: Use smtp.gmail.com with port 587 or 465
# Generate an App Password: https://myaccount.google.com/apppasswords
# Leave empty to disable email functionality
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM="SitePlod <siteplod@gmail.com>"

# ============================================
# Rate Limiting (OPTIONAL)
# ============================================
# Redis URL for distributed rate limiting
# Only required for production with multiple instances
# Leave empty to use in-memory rate limiting
REDIS_URL=

# ============================================
# Development (OPTIONAL)
# ============================================
# Set to 'development' for detailed error messages
# Set to 'production' for production deployments
NODE_ENV=development
